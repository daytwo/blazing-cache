# Blazing Cache (daytwo/blazing-cache)

Minimal Blitz-like static page cache plugin (DigitalOcean-first MVP). Place under `plugins/blazing-cache` and require via composer path repository for local development.

This is an early scaffold — it provides basic file storage and a DigitalOcean purger stub, plus simple Redis fallback for dependency tracking.
